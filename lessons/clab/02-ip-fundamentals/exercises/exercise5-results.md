# Deliverables

- A /30 mask at .200 cannot reach .1 because its range spans across only 4 addresses and within them onlu two are usable for hosts (.201 and .202).
- .1 is on another different network and a route (through a router) must be in place to enable connectivity. Subnet mask determine what is considered to be "local", i.e. reachable via L2 and what's considered to be "remote", i.e. needs a router and a route to enable connectivity.
